# WoundWatch data models (Pydantic schemas + status machine).
# Phase 1: models/schemas.py (domain shapes), models/status.py (CaseStatus + transitions).
# Phase 7: models/review.py (review request/response shapes).
