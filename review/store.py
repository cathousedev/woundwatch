"""Review store: query helpers. Phase 7.

list_pending(), get_for_review(case_id), record_decision(case_id, decision).
Approve/reject both go through the transition() helper with the reviewer as actor.
"""
