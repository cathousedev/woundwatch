"""Vision model adapters. Phase 3 (mock) / Phase 4 (MedGemma).

VisionModelAdapter ABC + MockMedGemmaAdapter + MedGemmaAdapter + get_adapter()
factory (reads config.vision_adapter). Every call logs an assessment_runs row.
"""
